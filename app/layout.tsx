import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Nexire — AI-Powered Recruitment Intelligence",
  description:
    "Source, reveal, and engage top talent with Nexire's intelligent recruitment platform. Powered by Prospeo data and AI scoring.",
  keywords: ["recruitment", "hiring", "talent sourcing", "AI", "Nexire"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
